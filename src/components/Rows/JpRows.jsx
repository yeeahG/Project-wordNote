import React from 'react'
import { useParams } from 'react-router-dom';
import Hiragana from '../Hiragana'
import Katakana from '../Katakana'

const JpRows = (props) => {
{/*}    const { id } = useParams();
    console.log(id);

    return (
        <div>
            
            <Hiragana />
            <Katakana />

        </div>
    )
    */}
}

export default JpRows